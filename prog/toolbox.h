int nb_char(unsigned char str[100]);
// Permet de compter les caractères présents dans une chaine

void replace(char * message);
// Permet de remplacer les espace par des '_' dans une chaine

void replace_letter(char old, char new, char * message);
// Permet de remplacer certaines lettres par d'autres dans une chaine

void upper(char * message);
// Permet de remplacer les maj par des min (et inversement) dans une chaine
